A chat bot framework written in Scala.

Now supports:

    scalex - http://scalex.org/
    ls     - http://ls.implicit.ly/
    scala  - REPL mode

To run:

    sbt "run user password server nick room"
